[package]
name = "calm-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
rxing = { version = "0.8.5", default-features = false, features = ["encoding_rs"] }
